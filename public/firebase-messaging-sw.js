// Basic stub for Firebase Cloud Messaging service worker.
// Replace with your real FCM setup if/when you integrate Firebase.

self.addEventListener('push', function (event) {
  // No-op handler to avoid errors; customise for real notifications.
  event.waitUntil(Promise.resolve());
});
